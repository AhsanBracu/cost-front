# Leave empty in local dev — the Vite dev server proxies /api and /uploads to
# http://localhost:3000 (see vite.config.ts), which avoids CORS issues since
# the backend has no CORS middleware configured.
# Set this to the deployed API origin when building for production.
VITE_API_URL=
